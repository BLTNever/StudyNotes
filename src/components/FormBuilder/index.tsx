/**
 * 通用表单组件
 */
import FormBuilder from './FormBuilder'

import { IProps as FormBuilderProps, EleProps as ElementProps } from './interface'

export default FormBuilder

export interface IProps extends FormBuilderProps {}
export interface EleProps extends ElementProps {}
